export type TVideoDB = {
    id: string,
    title: string,
    duration: number,
    upload_at: string
}

export type TVideoDBPost = {
    id: string,
    title: string,
    duration: number,
}

export type TCourseDB = {
    id: string,
    name: string,
    lessons: number
}